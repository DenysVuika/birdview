BEGIN;
CREATE TABLE IF NOT EXISTS projects (name TEXT, version TEXT, created_on TEXT NOT NULL, origin TEXT);
CREATE TABLE IF NOT EXISTS angular (project_id INTEGER NOT NULL, version TEXT);
CREATE TABLE IF NOT EXISTS ng_modules (project_id INTEGER NOT NULL, path TEXT NOT NULL, url TEXT);
CREATE TABLE IF NOT EXISTS ng_components (project_id INTEGER NOT NULL, path TEXT NOT NULL, standalone INTEGER, url TEXT);
CREATE TABLE IF NOT EXISTS ng_directives (project_id INTEGER NOT NULL, path TEXT NOT NULL, standalone INTEGER);
CREATE TABLE IF NOT EXISTS ng_services (project_id INTEGER NOT NULL, path TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS ng_pipes (project_id INTEGER NOT NULL, path TEXT NOT NULL, standalone INTEGER);
CREATE TABLE IF NOT EXISTS ng_dialogs (project_id INTEGER NOT NULL, path TEXT NOT NULL, standalone INTEGER);
CREATE TABLE IF NOT EXISTS warnings (project_id INTEGER NOT NULL, path TEXT NOT NULL, message TEXT NOT NULL, url TEXT);
CREATE TABLE IF NOT EXISTS packages (project_id INTEGER NOT NULL, path TEXT NOT NULL, url TEXT);
CREATE TABLE IF NOT EXISTS dependencies (project_id INTEGER NOT NULL, package_id INTEGER NOT NULL, name TEXT NOT NULL, version TEXT NOT NULL, dev INTEGER);
CREATE TABLE IF NOT EXISTS file_types (project_id INTEGER NOT NULL, name TEXT NOT NULL, count INTEGER);
CREATE TABLE IF NOT EXISTS unit_tests (project_id INTEGER NOT NULL, path TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS e2e_tests (project_id INTEGER NOT NULL, path TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS test_cases (test_id INTEGER NOT NULL, name TEXT NOT NULL);
COMMIT;
